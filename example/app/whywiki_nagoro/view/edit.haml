%a{:href => r(@page)}= h("< #@page")
%form{:method => :post, :action => r(:save)}
  %input{:type => :hidden, :name => :page, :value => h(@page)}
  %textarea{:name => :text}~ @text
  %input{:type => :submit, :value => 'Save'}
